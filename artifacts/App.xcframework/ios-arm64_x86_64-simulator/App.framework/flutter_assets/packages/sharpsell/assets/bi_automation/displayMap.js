function evaluateExpression() {
//    const result = `${60 - formData.insuredAge} years`;
    const result = `{customExpression}`;
    console.log(result);
    return result;
}

evaluateExpression();
